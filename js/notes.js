(()=>{const KEY='mrdream24-notes-v1';const defaults=[{id:'welcome-short',date:'2026-08-05T22:04:00+08:00',title:'',body:'有时候，阅读一本书只是为了确认自己仍然能够长时间地注视同一个问题。',tags:['阅读'],images:[]},{id:'welcome-medium',date:'2026-08-03T18:22:00+08:00',title:'',body:'最近重新玩了一遍《逆转裁判》。\n\n以前我更在意谜题能不能骗过我，现在却开始注意人物如何在一套高度程式化的类型结构里获得感情。类型小说真正困难的地方，也许不是制造意外，而是在规则内部仍然保留人的重量。',tags:['产品'],images:[]},{id:'welcome-long',date:'2026-07-29T20:15:00+08:00',title:'为什么我仍然需要一个自己的博客',body:'社交平台让表达变得容易，却也让每一句话迅速沉入时间线。一个自己的博客并不是为了恢复某种庄严的写作姿态，而是为了让零散的念头有一个不会被算法重新排序的位置。\n\n它可以只是一句话，也可以是一篇没有写完的长文。重要的不是篇幅，而是这些文字仍然属于同一条可以回望的个人时间。',tags:['生活'],images:[]}];function load(){try{const x=JSON.parse(localStorage.getItem(KEY)||'[]');return [...x,...defaults.filter(d=>!x.some(n=>n.id===d.id))].sort((a,b)=>new Date(b.date)-new Date(a.date))}catch{return defaults}}function esc(s=''){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function fmt(v){const d=new Date(v);return new Intl.DateTimeFormat('zh-CN',{year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).format(d).replaceAll('/','.')}function render(filter='all'){const root=document.querySelector('#notesList');if(!root)return;const list=load().filter(n=>filter==='all'||(n.tags||[]).includes(filter));document.querySelector('#emptyState').hidden=!!list.length;root.innerHTML=list.map(n=>{const len=(n.body||'').replace(/\s/g,'').length;const cls=len<=40?'is-short':len<=200?'is-medium':'is-long';const imgs=(n.images||[]).slice(0,4);const imageHtml=imgs.length?`<div class="note-images count-${imgs.length}">${imgs.map((src,i)=>`<img src="${src}" alt="${esc(n.title||'随笔图片')} ${i+1}" loading="lazy">`).join('')}</div>`:'';const more=len>600?`<a class="note-more" href="#" data-expand>展开全文</a>`:'';const body=len>600?esc(n.body.slice(0,420))+'…':esc(n.body);return `<article class="note ${cls}" data-id="${esc(n.id)}"><div class="note-meta"><time>${fmt(n.date)}</time>${(n.tags||[]).map(t=>`<span class="note-tag">#${esc(t)}</span>`).join('')}</div>${n.title?`<h2 class="note-title">${esc(n.title)}</h2>`:''}${imageHtml}<div class="note-body" data-full="${encodeURIComponent(n.body||'')}">${body}</div>${more}</article>`}).join('');root.querySelectorAll('[data-expand]').forEach(a=>a.onclick=e=>{e.preventDefault();const body=a.previousElementSibling;body.textContent=decodeURIComponent(body.dataset.full);a.remove()})}document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.classList.remove('is-active'));b.classList.add('is-active');render(b.dataset.filter)});render();window.NotesStore={KEY,load}})();